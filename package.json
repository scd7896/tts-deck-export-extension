{
  "name": "crb-extension",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "cd dist && zip -r ../crb-extension.zip . && cd ..",
    "start": "vite",
    "build": "vite build && vite build --config vite-content.config.ts"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/chrome": "^0.1.34",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
